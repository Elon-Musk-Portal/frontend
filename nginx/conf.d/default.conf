server {
    listen 80;
    rewrite ^(/\w+)-(?<myport>\d+)(\.\w+)$ /$myport$1$3;
    if ($myport != 3000) {
        return 301 http://127.0.0.1:3000;
    }
    location / {
        proxy_pass http://127.0.0.1:3000;
    }
}
